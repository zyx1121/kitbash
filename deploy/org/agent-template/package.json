{
  "name": "agent-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "kitbash agent kit: a Process that is an agent, answering a task by calling the tools of its owner's MCP surface.",
  "main": "index.js",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.127.0",
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": {
    "yaml": "2.9.0"
  }
}
