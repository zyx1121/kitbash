{
  "name": "kitbash-e2e-client",
  "version": "1.0.0",
  "private": true,
  "description": "The second MCP client of the end to end job: the TypeScript SDK against /mcp, run inside a Process's container. Installed with npm ci, so the lock file beside this one is what the job runs, transitive versions included.",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0"
  }
}
